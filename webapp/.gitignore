env/
__pycache__/
development-secrets.ini

# Fly deploy token. NEVER commit. Also has chmod 600.
.env

# Bundled pool data — Docker build artifact, synced by `make prepare-deploy`.
skills/

# Bakeoff generated artifacts (candidates, tasks, votes). The harness code is
# tracked; the generated data is local and disposable.
bakeoff/data/

# Test + tooling caches.
.coverage
.mypy_cache/
.pytest_cache/
.ruff_cache/
htmlcov/

# Bundled setting corpus - Docker build artifact, snapshot by `make prepare-deploy`.
setting/

# Campaign character cache - gitignored build artifact, refreshed by `make prepare-deploy`.
opcache/
