# GM request - feature 211

Received 2026-09-20, in the gm-assistant session "Discord repl". Reproduced verbatim, voice-dictation
artifacts included ("role" is "roll" throughout - see the project's mistranscription table). This is
the text the `spec-fidelity` review grades [spec.md](spec.md) against.

---

I am interested in updating my Discord integration in order to have more commands available. In particular, while we already have a `/etiquette` command, It would be great if there was a generic `/roll` command with auto completion where you could roll any skill and then also indicate void points and such. This would not need to handle discretionary bonuses that you apply after you see a role because you would be able to mark those on your character sheet after the role is posted.

it would also be nice if there was a slash command for individual skills like `/precepts` and `/sincerity` etc. I know there is some upper limit on how many commands a single bot can register, but I think it's something like 100. So I think that's totally fine for us to have individual slash commands for each individual skill.

We will leave out combat skills for now. But I do also want a /initiative command to roll initiative. like other roles that we make this should affect someone's character sheet so for example just as spending a void point when making a role from Discord it should actually mark the void point as spent on the character sheet (and someone should not be able to spend more void points than they currently have), Rolling initiative resets the combat round for that character And then we should be able to see the action dice on their character sheet if we go to it, etc.

This seems like a significant enough feature to warrant a spec kit feature, so please go ahead and plan this out.

---

## Message 2, 2026-09-20 (same session)

Sent after the session reported the plan, the two findings (void/initiative are browser code; an
open tab overwrites whole tracking state) and the two open questions. Verbatim.

---

There is one other type of pre-roll bonus that can be made, and that is the use of otherworldliness. I do have one current player who has otherworldliness, but I think that we can leave that off for now and get an initial implementation working and then add otherworldliness in a later feature. along with other school knacks, which are different and not as simple as making a normal roll. , And I agree that void on initiative is not allowed. However, I would like you to update the plan to include school knacks which get rolled, specifically we want `/oppose-social`, `/oppose-knowledge`, and `/commune` (which automatically spends a void point just to activate it).  We will hold off on other school knacks and school abilities for now, as I will specify what I want for those in a later feature.

It sounds as if we will need to make some changes to the actual character sheet. Application in order to do this. So what I would like you to do is plan out what changes would need to be made to the character sheet. In order for us to be able to make our implementation in a sensible way. For example, anything that is not currently exposed over the API of the character sheet should be updated so that it is exposed over the API. and things which are handled purely in front-end JavaScript can be moved. In fact, it sounds as if if I understand you correctly, that there are some things which are currently problematic if the character sheet is open multiple times. Like if two different tabs have the same character sheet open. That there is some chance of this kind of thing happening. And that should be fixed generally. I don't think that you need to do a full audit. of this, but what I want instead is for you to write up a set of requirements for the character sheet app. To implement and then I have a separate container in which I do my character sheet development. So I will pass along your write-up to that container. In fact, you can write that specification and then save it in the root directory of the character sheet Git repository. As a markdown file. And then I can tell the character sheet app to begin work doing that markdown file. Your set of requirements should include both what you specifically need in order to be able to implement this feature. And also the general requirement that the character sheet Claude code session should perform a full audit of the character sheet app to look for things such as this. which are purely implemented in front-end logic and which in particular would cause problems if there were two different browser tabs open or anything like that. Then I think our sequence of events can be something like me having the character sheet changes made and deployed. And then once you have everything you need there, you can implement this in a sensible way. We should not implement any kludgy workarounds when we can instead fix the character sheet app by passing along the specifications we need to the other Claude code session which is running in the other container. In this case, I do not believe that your session needs to communicate with that session directly because I believe that we can simply have you write a set of requirements and then have them implemented in the other container. If it ever becomes necessary to set up a communication channel between your two containers, then we can certainly do that. That is fairly trivial. I just don't think that it is worth the effort here, though you can let me know if you disagree. Therefore, I would like for you to write that set of requirements and then make sure that your own spec kit feature includes the understanding that this implementation of a character sheet app will already be completed as per what you need in order to do your work before you actually begin work on the spec kit feature. So the first task of the feature would be validating that this work was done. And then you will be able to move forward without any kludgy workarounds or without having to contend with the bugs that have just been exposed by your investigation.
