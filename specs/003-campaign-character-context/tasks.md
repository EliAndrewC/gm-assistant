# Tasks: Campaign Character Context

**Input**: Design documents from `/specs/003-campaign-character-context/`

**Prerequisites**: plan.md, spec.md, research.md, data-model.md, contracts/opcache.md, quickstart.md

**Tests**: INCLUDED - Principle X (NON-NEGOTIABLE) mandates red-green TDD + 100% coverage on the new module; the OP boundary is fixture-tested (Principle X.5).

**Organization**: The core machinery (op helpers + `opcache.py`) is shared by all stories, so it lives in Foundational; the user-story phases are the prompt wiring (US1/US2), the graceful route integration (US3), and the readout (US4).

**Path convention**: web app under `webapp/`; chargen sub-app at `webapp/chargen/`; tests beside code; fixtures in `webapp/chargen/fixtures/`.

## Format: `[ID] [P?] [Story] Description`

---

## Phase 1: Setup

- [X] T001 [P] Add `opcache/` to `webapp/.gitignore` (gitignored build artifact, like `setting/`/`skills/`).
- [X] T002 [P] In `webapp/Makefile`, extend the `cov` target to also measure the new module: `--cov=l7r --cov=chargen.brief --cov=chargen.opcache`. Confirm `chargen/opcache.py` is NOT added to any grace list in `webapp/pyproject.toml` (so it gets `mypy --strict` + full coverage).
- [X] T003 [P] Ensure `webapp/chargen/fixtures/` exists (created last feature); it will hold OP list + body fixtures.

---

## Phase 2: Foundational (core machinery - BLOCKS all stories)

**⚠️ CRITICAL**: The op helpers + `opcache.py` are needed by every user story.

- [X] T004 [P] In `webapp/chargen/op.py` (grace-listed boundary): add `updated_at` (keep name/tags) to each dict returned by `existing_characters()`; add `get_character_body(character_id)` doing `GET /campaigns/{id}/characters/{cid}.json`, returning `{bio, game_master_info, name, tags, updated_at}` or `None` on failure. Both fail soft (catch + `cherrypy.log`, never raise; `existing_characters` keeps returning `[]` on failure). Verify: importable; a live 1-char call returns a populated body dict.
- [X] T005 [P] Record OP fixtures into `webapp/chargen/fixtures/`: a saved list response (several characters incl. `updated_at`) and 2-3 saved body responses. Dash-normalize any typographic dashes in the saved text so the guard stays green. Verify: fixtures load as JSON.
- [X] T006 Write FAILING tests in `webapp/chargen/test_opcache.py` (red) for: `load_cache` (missing file -> `{}`; malformed -> `{}` logged), `save_cache` round-trip, `refresh` (new id fetches body; unchanged `updated_at` skips fetch; changed `updated_at` refetches; deleted id dropped; `body_fn` returning None keeps prior/omits), `assemble_context` (block format, count, case-insensitive self-exclusion, empty -> `('',0)`), `get_campaign_context` (TTL reuse via injected `now`; OP-down -> `('',0)`, never raises), `refresh_cache_file` (load->refresh->save). Verify: `pytest chargen/test_opcache.py` fails.
- [X] T007 Implement `webapp/chargen/opcache.py` to pass T006 (green): typed, ruff-clean, no broad excepts (the messy try/except lives in op.py); `updated_at` normalization before compare; cache path resolved relative to the module (`chargen/../opcache/characters.json`, i.e. `webapp/opcache` dev / `/app/opcache` prod); `logging` not `print`. Verify: `pytest chargen/test_opcache.py` passes; `ruff check`, `ruff format --check`, `mypy --strict`, and `--cov=chargen.opcache` = 100% all clean.

**Checkpoint**: cache machinery works and is fully covered; OP boundary fixture-tested.

---

## Phase 3: User Stories 1 & 2 - Consistency + reference (Priority: P1)

**Goal**: New backstories don't contradict the existing cast, and steering notes that name a character are grounded in that character's real backstory. (Same wiring delivers both.)

**Independent Test**: With known characters cached, synthesize a new one -> result is consistent with them; synthesize with a steering note naming one -> result reflects that character's backstory.

- [X] T008 [US1] In `webapp/chargen/synthesis.py` `build_prompt`: insert `# OTHER CAMPAIGN CHARACTERS\n\n<block>` between the SETTING BRIEF and CHARACTER sections when the block is non-empty (omit entirely when empty); the block is supplied by the caller (route) or fetched via `opcache.get_campaign_context(exclude_name=...)`. Exclude the character being generated by name. Verify: `build_prompt` includes the section when context is passed, omits it when empty; all prior sections intact.
- [X] T009 [US2] In `webapp/chargen/synthesis.py` INSTRUCTIONS: add a bullet - keep this character consistent with the OTHER CAMPAIGN CHARACTERS; when steering notes name one, ground the relationship in that character's stated backstory; never contradict them. Verify: bullet present; dash guard green.
- [X] T010 [P] [US1] Add tests in `webapp/chargen/test_synthesis.py`: `build_prompt` includes the campaign block + consistency instruction when context is provided, omits the section when context is empty, and still excludes-by-name upstream (assemble_context already covers exclusion). Verify: `pytest` green.

**Checkpoint**: the prompt carries the cast; consistency + reference behavior available.

---

## Phase 4: User Story 3 - Non-blocking, fast, graceful (Priority: P1)

**Goal**: Context gathering never blocks/fails synthesis and re-rolls don't re-gather.

**Independent Test**: OP unreachable -> synthesis still returns a backstory (context 0); rapid re-rolls reuse the cache within the TTL.

- [X] T011 [US3] In `webapp/chargen/website.py` `synthesize`: gather context via `opcache.get_campaign_context(exclude_name=<full_name>)`, pass the block into `synthesis.synthesize`/`build_prompt`, and wrap the gather so any failure yields empty context (never turns a successful synthesis into an error). Verify: route still returns `{ok:true,...}` when context gathering raises/returns empty.
- [X] T012 [US3] Add tests in `webapp/chargen/test_website_synthesize.py`: context-gather failure still yields an ok envelope; the gathered count is threaded through. (`opcache` TTL/OP-down already unit-tested in T006.) Verify: `pytest` green.

**Checkpoint**: reliability preserved; OP problems degrade to 0-context, never block.

---

## Phase 5: User Story 4 - Context readout (Priority: P2)

**Goal**: The GM sees how many campaign characters were in context.

**Independent Test**: After synthesis, a visible "N campaign characters in context" appears near the Backstory; reads 0 when OP is down.

- [X] T013 [US4] In `webapp/chargen/website.py` `synthesize`: add `context_count` (int) to the success envelope. Verify: envelope carries the count.
- [X] T014 [US4] In `webapp/chargen/templates/index.html`: show "N campaign characters in context" near the Backstory result; register any new dom id. Verify: renders with the count after synthesis.
- [X] T015 [US4] UI verification (Principle I + VI): render the chargen page, reveal the result with a sample `context_count`, run the screenshot suite + DOM audit at GM-100/200/tablet/mobile (zero issues), and a `frontend-review` pass. Verify: zero DOM issues; reviewer sign-off.

**Checkpoint**: readout visible and trustworthy; UI clean at all viewports.

---

## Phase 6: Polish & Cleanup

- [X] T016 In `webapp/Makefile` `prepare-deploy`: refresh + bundle the cache via `python3 -c "import l7r; from chargen import opcache; opcache.refresh_cache_file()"` (writes `webapp/opcache/characters.json`); in `webapp/Dockerfile` add `COPY opcache ./opcache`. Verify: `make prepare-deploy` writes the cache; Dockerfile copies it.
- [X] T017 Run the full gate from `webapp/`: `make done` (ruff + format + mypy --strict + pytest + 100% cov incl. `chargen.opcache`). Verify: green.
- [X] T018 Live smokes (quickstart): `refresh_cache_file()` populates the cache (full then incremental); `get_campaign_context('A New Name')` returns block + count>0 and excludes a passed existing name; OP-down -> `('',0)` and `synthesize` still returns a backstory; end-to-end synthesize with a steering note naming an existing character reflects its real backstory. Verify: all observed.
- [X] T019 [P] Update `webapp/CLAUDE.md` (note campaign-context in the synthesis section) and the project memory (feature shipped). Verify: no stale references; dash guard green.

---

## Dependencies & Execution Order

- **Setup (P1)**: T001-T003, independent.
- **Foundational (P2)**: T004/T005 `[P]`; T006 (red) before T007 (green); BLOCKS all stories.
- **US1/US2 (P3)**: after Foundational. T008 before T009; T010 `[P]`.
- **US3 (P4)**: after Foundational + T008 (route calls into build_prompt). T011 before T012.
- **US4 (P5)**: after T011 (envelope). T013 -> T014 -> T015.
- **Polish (P6)**: T016 anytime after T007; T017/T018 after all code; T019 `[P]`.

### Parallel opportunities
Setup all `[P]`; T004 ‖ T005; T010 `[P]`; T019 `[P]`.

## Implementation Strategy

1. Setup -> Foundational (cache machinery, fully tested) - the bulk of the work and the MVP core.
2. US1/US2 wiring - **STOP and VALIDATE**: a synthesis now carries the cast and honors named references.
3. US3 - confirm graceful/fast under OP failure + re-rolls.
4. US4 - the readout + UI verification.
5. Polish - bundle at deploy, `make done`, live smokes, docs.

### MVP scope
Foundational + US1/US2 (the cache feeding the prompt) is the demonstrable core; US3 hardens it, US4 surfaces trust, Polish ships it.

## Notes
- `opcache.py` is compliant (strict + 100% cov); the OP try/except mess stays in grace-listed `op.py` (fail-soft returns).
- Context gathering is never fatal - only a missing setting corpus fails loud.
- Cache is gitignored; a fresh checkout pays a one-time full pull. GM handles all git.
